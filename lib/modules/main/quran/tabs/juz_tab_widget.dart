import 'package:flutter/material.dart';

class JuzTabWidget extends StatelessWidget {
  const JuzTabWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('الاجزاء'));
  }
}
